---
title: PbButton
description: Versatile button component with three styles, color variants, PbIcon support, loading states and full design-system consistency.
---

# PbButton Widget

## Overview

The `PbButton` is the primary button component in the Propertybox design system. It comes in three distinct styles:

Figma reference: https://www.figma.com/design/24mSoGWIWxWpitijtkSJSN/Propertybox?node-id=42662-7701

- **primaryLarge**: prominent full-width call-to-action button (e.g. "Arrange Viewing", "Contact Agent")
- **secondary**: medium-sized button with multiple color options (e.g. "Save", "Share", "Enquire")
- **compact**: small pill-shaped button (e.g. tags, inline actions like "Featured")

It provides:
- Fully rounded (pill) corners
- Momo Trust font with sentence-case text
- Automatic icon tinting when using `PbIcon`
- Automatic icon sizing relative to label size
- Loading state with centered spinner
- Disabled state (via `onPressed: null` or `isLoading: true`)
- Optional fixed width or full-width behavior

<img 
  src="https://raw.githubusercontent.com/FocalAgent-Limited/propertybox-ui/main/docs/components/pb_button/preview-all-styles.png" 
  alt="PbButton – primary large, secondary variants, compact" 
  width="480"
/>

*(Please replace the image URL with an actual screenshot showing all three styles + color variations once uploaded)*

## Class Definition

```dart
enum PbButtonStyle {
  primaryLarge,   // prominent full-width CTA – 40px height
  secondary,      // flexible color & size – 30px height
  compact,        // small pill – 28px height
}

enum PbSecondaryColor {
  purple,   // #635CF5 – white text & icon
  teal,     // #0AD0AB – white text & icon
  dark,     // #0A101D – white text & icon
  white,    // white background + 1px dark border + dark text/icon
}

class PbButton extends StatelessWidget {
  final VoidCallback? onPressed;
  final String label;
  final PbButtonStyle style;
  final Widget? icon;               // PbIcon.xxx()
  final bool iconOnTrailing;
  final bool isLoading;
  final PbSecondaryColor? secondaryColor;
  final bool useWhiteVariant;       // white style for primaryLarge only
  final double? width;
  final Color? iconColor;           // optional explicit icon tint override

  const PbButton({
    super.key,
    required this.onPressed,
    required this.label,
    required this.style,
    this.icon,
    this.iconOnTrailing = false,
    this.isLoading = false,
    this.secondaryColor,
    this.useWhiteVariant = false,
    this.width,
    this.iconColor,
  });

  // ... full implementation in pb_button.dart ...
}
```

## Properties
- `onPressed` (`VoidCallback?`, required): The callback function triggered when the button is tapped. Pass `null` to disable the button.
- `label` (`String`, required): The text displayed on the button (automatically converted to sentence case).
- `style` (`PbButtonStyle`, required): Defines the visual style of the button. Must be one of: `primaryLarge`, `secondary`, or `compact`.
- `icon` (`Widget?`, optional): A `PbIcon` widget to display inside the button. The icon is automatically tinted to match the button's text color.
- `iconOnTrailing` (`bool`, optional, default: `false`): If `true`, places the icon after the label text instead of before it.
- `isLoading` (`bool`, optional, default: `false`): When `true`, shows a centered loading spinner instead of the label and icon.
- `secondaryColor` (`PbSecondaryColor?`, optional): Sets the background and text/icon color when `style` is `secondary`. Defaults to purple if omitted.
- `useWhiteVariant` (`bool`, optional, default: `false`): When `true` and `style` is `primaryLarge`, uses a white background with dark text/icon and 1px border.
- `width` (`double?`, optional): Forces the button to a specific width in logical pixels. If omitted, `primaryLarge` stretches full-width and other styles shrink to content.
- `iconColor` (`Color?`, optional): Overrides automatic icon tinting when an icon is supplied.

## Usage
Use `PbButton` in your UI like this:

### Basic primary large button (full width)
```dart
PbButton(
  style: PbButtonStyle.primaryLarge,
  label: 'Arrange Viewing',
  icon: PbIcon.campaign(),
  onPressed: () {
    // handle tap
  },
)
```
### Primary large – white variant + fixed width
```dart
PbButton(
  style: PbButtonStyle.primaryLarge,
  label: 'Contact Agent',
  icon: PbIcon.login(),
  useWhiteVariant: true,
  width: 240,
  onPressed: () {},
)
```

### Secondary buttons with different colors
```dart
Row(
  mainAxisAlignment: MainAxisAlignment.center,
  children: [
    PbButton(
      style: PbButtonStyle.secondary,
      label: 'Save',
      icon: PbIcon.star(),
      secondaryColor: PbSecondaryColor.teal,
      onPressed: () {},
    ),
    const SizedBox(width: 16),
    PbButton(
      style: PbButtonStyle.secondary,
      label: 'Share',
      icon: PbIcon.switchButton(),
      secondaryColor: PbSecondaryColor.white,
      onPressed: () {},
    ),
  ],
)
```

### Trailing icon usage
```dart
PbButton(
  style: PbButtonStyle.secondary,
  label: 'Save Search',
  icon: PbIcon.star(),
  iconOnTrailing: true,
  secondaryColor: PbSecondaryColor.white,
  onPressed: () {},
)
```

### Compact pill button
```dart
PbButton(
  style: PbButtonStyle.compact,
  label: 'Featured',
  onPressed: () {},
)
```

### Loading & disabled states
```dart
PbButton(
  style: PbButtonStyle.primaryLarge,
  label: 'Processing...',
  isLoading: true,
  onPressed: null, // also disables
)
```

## Web Preview (HTML/CSS/JS)

```html
<div class="pb-button-demo">
  <button class="pb-btn pb-btn-primary" id="primaryBtn">Arrange Viewing</button>
  <button class="pb-btn pb-btn-secondary-teal">Save</button>
  <button class="pb-btn pb-btn-secondary-white">Share</button>
  <button class="pb-btn pb-btn-compact">Featured</button>
</div>
```

```css
.pb-button-demo {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
  font-family: Poppins, sans-serif;
}

.pb-btn {
  border-radius: 999px;
  border: 0;
  cursor: pointer;
  font-weight: 500;
  transition: transform 0.12s ease, opacity 0.12s ease;
}

.pb-btn:active { transform: scale(0.98); }

.pb-btn-primary {
  min-width: 220px;
  height: 40px;
  padding: 0 24px;
  font-size: 15px;
  color: #fff;
  background: #635cf5;
}

.pb-btn-secondary-teal,
.pb-btn-secondary-white {
  height: 30px;
  padding: 0 20px;
  font-size: 11px;
}

.pb-btn-secondary-teal {
  color: #fff;
  background: #0ad0ab;
}

.pb-btn-secondary-white {
  color: #0a101d;
  background: #fff;
  border: 1px solid #0a101d;
}

.pb-btn-compact {
  height: 28px;
  padding: 0 16px;
  font-size: 12px;
  color: #0a101d;
  background: #fff;
  border: 1.5px solid #0a101d;
}

.is-loading {
  opacity: 0.7;
  pointer-events: none;
}
```

```javascript
const primaryBtn = document.getElementById('primaryBtn');

primaryBtn.addEventListener('click', () => {
  primaryBtn.classList.add('is-loading');
  const original = primaryBtn.textContent;
  primaryBtn.textContent = 'Processing...';

  setTimeout(() => {
    primaryBtn.classList.remove('is-loading');
    primaryBtn.textContent = original;
  }, 1200);
});
```

## Refactor Notes

Current implementation is production-ready and should remain stable for now.

Candidates to evaluate in a later refactor:
- Consider replacing the many optional constructor parameters with a style/config object for each button variant.
- Confirm whether `secondaryColor` + `useWhiteVariant` should be unified into a single color/appearance model.
- Keep `PbButtonStyle` as the primary semantic API to preserve design-system consistency.
- Ensure all visual states (default, hover, pressed, disabled, loading) are documented and test-covered for each style.